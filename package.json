{
  "name": "mneme-plugin",
  "private": true,
  "description": "Distribution repo for the mneme Claude Code plugin. Holds hand-written packaging; the compiled server binary is produced by the mneme code repo and gitignored.",
  "scripts": {
    "test": "node scripts/validate-manifests.mjs && node scripts/check-skill-names.mjs && node scripts/check-skill-handoff.mjs && node scripts/check-anchor-rules.mjs && node scripts/check-design-checker.mjs && node scripts/check-design-lint.mjs && node scripts/check-session-tokens.mjs && node scripts/check-release-pin.mjs && node scripts/check-workflows.mjs && node scripts/check-readme.mjs && sh scripts/check-launcher.sh",
    "validate-manifests": "node scripts/validate-manifests.mjs",
    "reinstall": "bash scripts/reinstall.sh"
  }
}
