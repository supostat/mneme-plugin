# Generated binary — produced by scripts/build-plugin.ts in the mneme CODE repo.
# ~62 MB, reproducible from source, never committed. Only the binary itself is
# ignored: the launcher and the release pin beside it ARE tracked.
plugin/bin/mneme

# dev-workflow / engram runtime + session data (machine-local, not distribution)
.engram/

# dev-workflow tooling (machine-specific settings + synced/version-locked skills)
.claude/

# Local MCP registration — absolute developer paths, does not register mneme
.mcp.json

# Editor-local settings
.vscode/

# Secrets
.env
.env.*

# OS / editor cruft
.DS_Store
*.swp
*.swo
*~
CLAUDE.md
docs
.dev-vault
